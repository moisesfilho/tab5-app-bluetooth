# Tab5 Bluetooth App (`tab5-app-bluetooth`)

Aplicativo isolado de gerenciamento Bluetooth e periféricos para o **Tab5 OS**.

## Compilação e Empacotamento

```bash
chmod +x tools/build.sh
./tools/build.sh
```

O pacote `.tab5pkg` será gerado em `dist/com.tab5.bluetooth.tab5pkg`.
